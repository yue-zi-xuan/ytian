<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.gcsj.mapper.NewsMapper">
    <select id="getNewsLike" resultType="com.gcsj.pojo.News" parameterType="String">
--         select * from news
--         where
--             title like "%"#{title}"%";
select * from news where title like CONCAT('%',#{title},'%');
    </select>
</mapper>